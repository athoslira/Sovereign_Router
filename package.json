{
	"name": "sovereign-router",
	"version": "1.6.0",
	"description": "Local-first Obsidian AI control plane with governed Hermes execution, vault context, and image tools.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint .",
		"test": "tsc -p tsconfig.test.json && node tests/build-tests.mjs",
		"test:bridge": "cd runtime-bridge && python -m unittest discover -s tests -v",
		"test:all": "npm test && npm run test:bridge && npm run build",
		"test:release": "node tests/verify-release-artifacts.mjs"
	},
	"keywords": [],
	"license": "0-BSD",
	"devDependencies": {
		"@eslint/js": "^9.39.4",
		"@types/node": "^22.15.17",
		"esbuild": "0.25.5",
		"eslint": "^9.39.4",
		"eslint-plugin-obsidianmd": "^0.4.0",
		"globals": "^17.6.0",
		"jiti": "^2.6.1",
		"obsidian": "latest",
		"typescript": "^5.8.3",
		"typescript-eslint": "^8.59.1"
	},
	"dependencies": {
		"@toon-format/toon": "^4.1.1"
	}
}
